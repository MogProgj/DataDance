/**
 * Queue implementations.
 *
 * <p>Planned:
 * <ul>
 *   <li>Circular array queue — backed by a circular buffer</li>
 *   <li>Linked queue         — backed by a singly linked list</li>
 *   <li>Two-stack queue      — backed by two stacks</li>
 * </ul>
 */
package structlab.core.queue;
